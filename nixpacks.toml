[phases.setup]
nixPkgs = ["jdk21_headless"]

[phases.build]
cmds = ["chmod +x gradlew && ./gradlew bootJar -x test"]

[start]
cmd = "java -Dspring.profiles.active=prod -jar build/libs/airsense-0.0.1-SNAPSHOT.jar"
